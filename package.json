{
  "name": "web-app",
  "packageManager": "bun@1.3.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["apps/*", "apps/*/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.6.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
